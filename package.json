{
  "name": "cricbuzz-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Root helper scripts for API and web apps.",
  "scripts": {
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev",
    "start:api": "npm --prefix api run start",
    "build:web": "npm --prefix web run build",
    "check:api": "npm --prefix api run check"
  }
}
